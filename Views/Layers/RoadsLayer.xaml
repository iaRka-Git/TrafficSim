<UserControl x:Class="TrafficSim.Views.Layers.RoadsLayer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid>
        <Canvas>
            <Line X1="{Binding PreviewRoadX1}" Y1="{Binding PreviewRoadY1}"
                  X2="{Binding PreviewRoadX2}" Y2="{Binding PreviewRoadY2}"
                  Stroke="DarkOrange" StrokeThickness="3" StrokeDashArray="4 2"
                  Visibility="{Binding IsPreviewingRoad, Converter={StaticResource BoolToVis}}" />
        </Canvas>

        <ItemsControl ItemsSource="{Binding VisualRoads}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ItemsControl ItemsSource="{Binding VisualLanes}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Polyline Points="{Binding ScreenPoints}" 
                                          Stroke="#555555" 
                                          StrokeThickness="{Binding ScreenThickness}" 
                                          StrokeLineJoin="Round"
                                          Cursor="Hand">
                                    <Polyline.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="Додати точку вигину" Command="{Binding DataContext.AddControlPointCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding Model.ParentGroup}"/>
                                        </ContextMenu>
                                    </Polyline.ContextMenu>
                                </Polyline>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>