<UserControl x:Class="TrafficSim.Views.Layers.GridLinesLayer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             IsHitTestVisible="False">
    <Grid>
        <ItemsControl ItemsSource="{Binding VerticalLines}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas ClipToBounds="True" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Rectangle x:Name="LineRect" 
                               Fill="#FFD3D3D3" Opacity="0.3" Width="1"
                               Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=ItemsControl}}">
                        <Rectangle.RenderTransform>
                            <TranslateTransform X="{Binding ScreenPos}" Y="0" />
                        </Rectangle.RenderTransform>
                    </Rectangle>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsMajor}" Value="True">
                            <Setter TargetName="LineRect" Property="Fill" Value="#FF909090"/>
                            <Setter TargetName="LineRect" Property="Opacity" Value="0.6"/>
                            <Setter TargetName="LineRect" Property="Width" Value="1.5"/>
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <ItemsControl ItemsSource="{Binding HorizontalLines}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas ClipToBounds="True" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Rectangle x:Name="LineRect" 
                               Fill="#FFD3D3D3" Opacity="0.3" Height="1"
                               Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ItemsControl}}">
                        <Rectangle.RenderTransform>
                            <TranslateTransform X="0" Y="{Binding ScreenPos}" />
                        </Rectangle.RenderTransform>
                    </Rectangle>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsMajor}" Value="True">
                            <Setter TargetName="LineRect" Property="Fill" Value="#FF909090"/>
                            <Setter TargetName="LineRect" Property="Opacity" Value="0.6"/>
                            <Setter TargetName="LineRect" Property="Height" Value="1.5"/>
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>